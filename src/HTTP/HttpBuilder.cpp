#include "HTTP/HttpBuilder.hpp"

namespace Wire
{
    namespace HTTP
    {
        std::string Builder::build(const Core::Request& req) {
            
        }
    } // namespace HTTP
} // namespace Wire
